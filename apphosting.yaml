# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0

# Environment variables and secrets.
env:
  # --- SECRETS ---
  # These are your Stripe API keys. They are SECRET.
  # Set their values using the `firebase apphosting:secrets:set` commands.
  - variable: STRIPE_API_KEY
    secret: STRIPE_API_KEY
  - variable: STRIPE_WEBHOOK_SECRET
    secret: STRIPE_WEBHOOK_SECRET

  # --- PUBLIC IDs ---
  # These are your Stripe Price IDs. They are NOT secret.
  # You can safely paste their values directly here.
  # Find these in your Stripe Dashboard under Products.
  - variable: STRIPE_PLUS_MONTHLY_ID
    value: "price_1T4OHgK0UI9TeZnH1NEUNaNF" # <-- REPLACE THIS VALUE
  - variable: STRIPE_ULTIMATE_MONTHLY_ID
    value: "price_1T4OIAK0UI9TeZnH8FzhMmcM" # <-- REPLACE THIS VALUE
  - variable: STRIPE_TOKEN_PRICE_ID
    value: "price_1T4OIbK0UI9TeZnHOJNK9x9j" # <-- REPLACE THIS VALUE
  - variable: STRIPE_BOOST_PRICE_ID
    value: "price_1T4OJ7K0UI9TeZnHPKuFevmG" # <-- REPLACE THIS VALUE
